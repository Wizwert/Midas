<UserControl x:Class="Midas.UI.Wpf.SecuritiesList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:wpf="clr-namespace:Midas.UI.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" d:DataContext="{d:DesignInstance wpf:SecuritiesListViewModel}">
    <Grid>
        <ListView ItemsSource="{Binding Securities}" DisplayMemberPath="Symbol">
            
        </ListView>
    </Grid>
</UserControl>
